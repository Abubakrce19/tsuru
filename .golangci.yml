run:
  deadline: 30m
  tests: true

linters-settings:
  govet:
    check-shadowing: true
  gofmt:
    simplify: true
  fieldalignment:
    suggest-new: true

linters:
  enable:
    - goimports
    - gofmt
    - misspell
    - fieldalignment
  disable:
    - errcheck
